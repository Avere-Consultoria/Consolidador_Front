node_modules/
dist/
.claude/
# lockfile fora do repo: gerado no Windows trava o rollup errado no CI Linux
# (npm bug de optionalDependencies). Sem lock, o Cloudflare faz npm install fresco.
package-lock.json
# Exceção: o build da lib avere-ui é vendorizado de propósito (necessário no CI/Cloudflare).
!vendor/avere-ui/dist/
!vendor/avere-ui/dist/**
.env
.env.local
.env.*.local
public/mocks/
*.key
*.crt
*.pfx
.DS_Store

eslint_report.json
eslint_results.json
lint_output.txt
